<ul>
	<% @products.each do | product | %>
		<li>
			<%= product.title %><br>
			<%= product.description %><br>
			<%= product.deadline %><br>
		</li>
	<% end %>
</ul>

<%= link_to("New Product", new_user_product_path(@user)) %>